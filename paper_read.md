## 语义分割
### CTFNet：同UNET框架
    特征提取 ： 用更强的CNN网络
    解码器： W/P MHSA，全局上下文信息并降低计算成本
    skip connection ：CDASPP（可分离空洞卷积），强制关注不同范围上下文，从简单拼接到自适应加权融合。
    tips： CNN注重细节、Transformer注重全局信息。 融合不能简单地add/concat 

### PMTSeg：任务自适应遥感图像分割的提示驱动多模态 Transformer
    多模态数据对齐：双流编码器 + JS散度 约束“亲和力矩阵”，不直接简单拼接
    多尺度融合：交叉注意力机制，提取不同尺度信息。
    解码：用类别作为Query去找像素。
    对比学习损失函数：利用“文本语义”先验知识保证稀有类别的特征。

### SAM2Former：用于遥感图像语义分割的 SAM2辅助UNet型Transformer
    SAM2Former采用双编码器-单解码器结构，由高效的ResNet18和SAM2的图像编码器作为双编码器，并以Transformer作为解码器。首先，引入轻量级的Adapter对SAM2进行参数高效微调，并设计了一个多尺度信息聚合模块(MIAM)来连接双编码器，该模块对SAM2 Block和CNN的特征进行逐层多尺度加权，从而保留图像中的关键信息。其次，我们设计了一个基于全局-局部Transformer模块(GLTM)的解码器，以有效提取全局上下文信息和局部细节信息，提高边缘纹理的分割能力。构建了特征增强模块(FEM)，通过结合通道和空间注意力特征来提高相似类别的识别精度。
    基于 Adapter 微调的 SAM2 编码器 (SAM2-based Encoder with PEFT)
    多尺度信息聚合模块 (MIAM)：NN提取的特征（多边缘纹理）与SAM2提取的特征（多高级语义）具有互补性。MIAM 用于将这两种特征进行逐层、多尺度的加权融合
    全局-局部 Transformer 解码模块 (GLTM)：增强边缘纹理的分割能力并防止掩码填充不完整，GLTM 并行地结合了局部和全局自注意力机制
    特征增强模块 (FEM)：对易混淆类别的识别精度



### EANV:面向遥感图像语义分割的边缘注意力网络与VMamba模块